import 'package:flutter/widgets.dart';

class TotalByDay extends StatelessWidget {
  const TotalByDay({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
